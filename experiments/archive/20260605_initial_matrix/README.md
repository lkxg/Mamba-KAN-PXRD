# 2026-06-05 Initial Matrix

Historical exploratory matrix before the clean E02 loss ablation.

Primary files:

- `results.md`: compact result table.
- `results.csv`: full result table.
- `logs/`: train/eval logs from the original runs.
- `config_snapshots/`: W&B-captured config snapshots from the original runs.

Notes:

- This archive is kept for context only.
- Some checkpoints referenced by `results.csv` are no longer present in the
  current worktree. The current reproducible comparison is
  `experiments/e02_loss_ablation/`.

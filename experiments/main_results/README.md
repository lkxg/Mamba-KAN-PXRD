# Legacy Active Output

This directory is kept only for the experiment chain that was already running
when the results layout changed. Do not start new runs here.

The canonical table is `../results.md`; new transient logs go to
`../../runs/experiment_logs/`. After the active chain finishes and its final
metrics are migrated, this directory can be removed.
